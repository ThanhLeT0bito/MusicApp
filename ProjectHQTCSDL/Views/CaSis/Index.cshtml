@model IEnumerable<ProjectHQTCSDL.Models.CaSi>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tenCaSi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.imgCaSi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tenCaSi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.imgCaSi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCaSi }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCaSi }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCaSi })
        </td>
    </tr>
}

</table>
